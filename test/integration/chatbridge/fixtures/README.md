# ChatBridge Fixture Space

Use this folder for reusable checked-in fixtures such as:

- reviewed app manifests
- bridge initialization envelopes
- lifecycle state-update payloads
- completion payloads
- degraded/error payloads

Fixtures should be deterministic, secretless, and safe to commit.
