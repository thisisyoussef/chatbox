# Bug Fix Batch

Use this ledger only when a task is intentionally being handled as a bounded bug
batch.

| Bug ID | Symptom | Expected Behavior | Evidence Source | Regression Coverage | Touched Files | Status |
|---|---|---|---|---|---|---|
| BUG-001 | `pnpm dev:web` intermittently throws a Vite overlay saying `Failed to resolve import "chess.js"` when `/dev/chatbridge` loads on a cold dependency-optimizer cache. | The web-only dev server should start without missing-module overlays, and the chess-backed ChatBridge surfaces should load cleanly on first open. | User report plus local reproduction from `pnpm dev:web` in `/private/tmp/chatbox-cb-003-parallel` and `/private/tmp/chatbox-chessjs-devfix`. | `src/electron.vite.config.test.ts` plus cold-start `pnpm dev:web` verification after clearing `node_modules/.vite`. | `electron.vite.config.ts`, `src/electron.vite.config.test.ts` | Fixed |
| BUG-002 | `pnpm dev:web` crashes in the Electron main process with `Cannot read properties of null (reading 'allowReportingAndTracking')` during `initSentry()`. | Desktop bootstrap should tolerate stored `null` settings values by rehydrating defaults before any main-process consumer reads them. | User stack trace plus local reproduction from `pnpm dev:web` in `/private/tmp/chatbox-chessjs-devfix` after the chess resolver fix. | `src/main/store-node.test.ts` plus live `pnpm dev:web` startup verification against the existing user-data store. | `src/main/store-node.ts`, `src/main/store-node.test.ts` | Fixed |
| BUG-003 | React logs `Warning: React does not recognize the disableEnforceFocus prop on a DOM element` when the main sidebar or thread-history drawer renders. | Drawer focus-trap overrides should be passed through MUI `ModalProps`, not leaked as unknown DOM props. | User browser console report against `Sidebar` and `ThreadHistoryDrawer` plus local component-level reproduction. | `src/renderer/Sidebar.test.tsx` and `src/renderer/components/session/ThreadHistoryDrawer.test.tsx` assert the prop is no longer passed at the top level and is present inside `ModalProps`. | `src/renderer/Sidebar.tsx`, `src/renderer/components/session/ThreadHistoryDrawer.tsx`, `src/renderer/Sidebar.test.tsx`, `src/renderer/components/session/ThreadHistoryDrawer.test.tsx` | Fixed |
| BUG-004 | Radix logs accessibility warnings that `DialogContent` requires a `DialogTitle` and description when `CommandDialog` opens. | The command dialog should always provide accessible title and description wiring, even when callers only pass command content. | User browser console report plus focused `CommandDialog` reproduction in jsdom. | `src/renderer/components/ui/command.test.tsx` verifies the warning strings are absent after render. | `src/renderer/components/ui/command.tsx`, `src/renderer/components/ui/command.test.tsx` | Fixed |
| BUG-005 | Renderer-only import/setup failures surface late as Vite overlays after the affected route renders. | `pnpm dev`, `pnpm test`, `pnpm check`, and `pnpm build` should stop immediately with a clear Node/install failure before Vite reaches route rendering when the worktree is stale or the shell is on the wrong Node major. | `/private/tmp/chatbox-cb-003-parallel` reproduced `pnpm run check/build` under Node v25.5.0, plus the reported Vite overlay for `chess.js` import resolution. | `test/integration/scripts/workspace-guard.test.ts` plus manual validation through `pnpm test`, `pnpm check`, and `pnpm build`. | `package.json`, `.erb/scripts/postinstall.cjs`, `scripts/workspace-guard.mjs`, docs/memory files. | Fixed |
| BUG-006 | `pnpm check` failed on `main`-adjacent branches because renderer routes, OSS OAuth stubs, image-generation records, and provider-registry contracts had drifted out of sync after upstream refactors. | The repo should type-check cleanly, with shared settings/image/provider contracts aligned to the current renderer/store call sites and open-source stubs matching the signatures expected by the UI. | Local `pnpm check --pretty false` reproduction on `codex/pnpm-check-drift-pass-2` after PR #51 merged. | Full baseline validation plus the existing `src/renderer/stores/imageGenerationActions.test.ts`, `src/shared/providers/definitions/models/openai-responses.test.ts`, and `src/shared/models/index.test.ts` coverage. | `src/shared/types/settings.ts`, `src/shared/defaults.ts`, `src/shared/types/image-generation.ts`, `src/renderer/routes/settings/web-search.tsx`, `src/renderer/routes/image-creator/index.tsx`, `src/renderer/storage/SQLiteImageGenerationStorage.ts`, `src/renderer/hooks/useOAuth*.ts`, `src/renderer/utils/homeWelcomeCard.ts`, `src/renderer/stores/session/crud.ts`, `src/shared/providers/**`, `src/shared/models/**`, `src/*/adapters/index.ts` | Fixed |
| BUG-007 | A later clarify route artifact could replace a still-active Chess runtime in the floating app tray because the tray selector treated every `app` part in `launching`, `ready`, or `active` as a live runtime candidate. | The tray should stay pinned to the live runtime surface and ignore clarify/refuse route artifacts that only explain the routing decision in the timeline. | User report with screenshot showing Chess replaced by the `Choose the next step` clarify card after asking to move the black queen. | `src/renderer/components/chatbridge/floating-runtime.test.ts` proves a later clarify artifact no longer displaces an active Chess runtime. | `src/renderer/components/chatbridge/floating-runtime.ts`, `src/renderer/components/chatbridge/floating-runtime.test.ts` | Fixed |
| BUG-008 | After `open the weather app` prompts for a location and the user replies with a specific location like `Detroit, Michigan`, the host stops at `Weather Dashboard route ready...` instead of opening the reviewed Weather Dashboard. A later `ok open it` follow-up then falls into a generic `Keep this in chat` receipt. | Resolving a valid location should immediately promote the weather flow into the reviewed Weather Dashboard launch contract. Legacy `route-ready` receipts should also honor an explicit launch confirmation follow-up. | User screenshots from 2026-04-03 plus local code-path inspection in `resolveWeatherDashboardTurn()` and `submitNewUserMessage()`. | Focused Vitest coverage for weather host routing and session submission, plus existing reviewed Weather launch scenario coverage. | `src/shared/weather-dashboard/intent.ts`, `src/renderer/packages/weather-dashboard/host-routing.ts`, `src/renderer/stores/session/messages.ts`, targeted weather tests. | Fixed |

## Validation Notes

- Focused regression: `pnpm exec vitest run src/electron.vite.config.test.ts`
- Additional verification: removed `node_modules/.vite`, started `pnpm dev:web`, confirmed the renderer came up without any `chess.js` import-analysis error, then verified `http://localhost:1212/dev/chatbridge` returned `200 OK`
- Focused regression: `pnpm exec vitest run src/main/store-node.test.ts`
- Additional verification: started `pnpm dev:web`, confirmed the previous `initSentry()` null-settings crash no longer appeared, then verified `http://localhost:1212/dev/chatbridge` returned `200 OK`
- Focused regression: `pnpm exec vitest run src/renderer/Sidebar.test.tsx src/renderer/components/session/ThreadHistoryDrawer.test.tsx src/renderer/components/ui/command.test.tsx`
- Additional verification: restarted `pnpm dev:web` from `/private/tmp/chatbox-ui-warning-cleanup`, confirmed `http://localhost:1212/dev/chatbridge` returned `200 OK`, and confirmed the app does not reference `index.js.map`; unknown sourcemap requests are falling through to the SPA HTML shell rather than indicating a tracked source-map artifact in repo code
- Focused regression: `pnpm vitest run test/integration/scripts/workspace-guard.test.ts`
- Additional verification: `pnpm install` records `node_modules/.chatbox-workspace-stamp.json`; `pnpm test`, `pnpm check`, and `pnpm build` now invoke `pnpm run guard:workspace` before the underlying toolchain.
- Focused regression: `pnpm check --pretty false` on `codex/pnpm-check-drift-pass-2` to confirm the remaining drift clusters cleared in one batch.
- Additional verification: `pnpm test` confirmed image-generation and provider-registry suites still pass after the shared contract reconciliation, including `src/renderer/stores/imageGenerationActions.test.ts` and `src/shared/providers/definitions/models/openai-responses.test.ts`.
- Focused regression: `source ~/.nvm/nvm.sh && nvm use 20 >/dev/null && pnpm exec vitest run src/renderer/components/chatbridge/floating-runtime.test.ts`
- Additional verification: `source ~/.nvm/nvm.sh && nvm use 20 >/dev/null && pnpm test`, `pnpm check`, `pnpm lint`, `pnpm build`, and `git diff --check` all passed from `/private/tmp/chatbox-fix-app-tray-runtime-pin`.
- Full validation: `pnpm check`, `pnpm lint`, `pnpm test`, `pnpm build`, `git diff --check`
- Focused regression: `source ~/.nvm/nvm.sh && nvm use 20 >/dev/null && pnpm run generate:routes && pnpm vitest run src/renderer/packages/weather-dashboard/host-routing.test.ts src/renderer/stores/session/messages.weather.test.ts`
- Additional verification: `source ~/.nvm/nvm.sh && nvm use 20 >/dev/null && pnpm vitest run src/renderer/packages/weather-dashboard/host-routing.test.ts src/renderer/stores/session/messages.weather.test.ts src/renderer/packages/chatbridge/reviewed-app-launch.test.ts test/integration/chatbridge/scenarios/live-reviewed-app-invocation.test.ts`
- Full validation: `source ~/.nvm/nvm.sh && nvm use 20 >/dev/null && pnpm test && pnpm check && pnpm lint && pnpm build && git diff --check`
