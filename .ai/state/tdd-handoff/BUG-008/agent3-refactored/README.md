## Agent 3 Review / Refactor

- Kept the fix bounded to weather host routing and session submission.
- Reused existing reviewed-app host tool wrapping instead of inventing a second launch record shape.
- Verified the generic reviewed launch shell and live weather reviewed-app scenario still pass unchanged.
